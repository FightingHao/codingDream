var arr1 = [1, 2, 3]

// 可传多个参数
var result = arr1.reverse()

// 返回颠倒后数组
console.log(result) // [3, 2, 1]

// 改变原数组
console.log(arr1) // [3, 2, 1]

// 返回值等于原数组
console.log(arr1 === result) // true